[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "superspec"
version = "0.1.0"
description = "SuperSpec prototype orchestrator"
requires-python = ">=3.10"

[project.scripts]
superspec = "superspec.cli:main"

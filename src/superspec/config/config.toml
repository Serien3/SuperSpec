[agents]
max_threads = 6
max_depth = 1

[agents.code-reviewer]
description = "Use this agent when a major project change has been completed and needs to be reviewed against the original plan and coding standards."
config_file = "../agents/code-reviewer.toml"
